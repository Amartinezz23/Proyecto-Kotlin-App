<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:background="@drawable/gradient_background"
    tools:context=".MainActivity">

    <!-- Título principal -->
    <TextView
        android:id="@+id/txtTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mi Mundo Android 🌍"
        android:textSize="28sp"
        android:textColor="#2C3E91"
        android:textStyle="bold"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="40dp" />

    <!-- Grid para las imágenes -->
    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:rowCount="2"
        android:columnCount="2"
        android:alignmentMode="alignMargins"
        android:rowOrderPreserved="false"
        android:useDefaultMargins="true"
        android:layout_gravity="center"
        android:layout_margin="24dp">

        <!-- App Youtube -->
        <ImageButton
            android:id="@+id/btnWeb"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_margin="20dp"
            android:background="@android:color/transparent"
            android:src="@drawable/youtube"
            android:scaleType="centerInside"
            android:contentDescription="Abrir Una app, Youtube"
            android:onClick="abrirWeb" />

        <!-- Url -->
        <ImageButton
            android:id="@+id/btnUrl"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_margin="20dp"
            android:background="@android:color/transparent"
            android:src="@drawable/url"
            android:scaleType="centerInside"
            android:contentDescription="Abrir Una Url"
            android:onClick="abrirUrl" />

        <!-- Teléfono -->
        <ImageButton
            android:id="@+id/btnPhone"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_margin="20dp"
            android:background="@android:color/transparent"
            android:src="@drawable/telefono"
            android:scaleType="centerInside"
            android:contentDescription="Llamar"
            android:onClick="abrirTelefono" />

        <!-- Alarma -->
        <ImageButton
            android:id="@+id/btnAlarma"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_margin="20dp"
            android:background="@android:color/transparent"
            android:src="@drawable/alarma"
            android:scaleType="centerInside"
            android:contentDescription="Abrir Alarma"
            android:onClick="PonerAlarma" />

        <!-- Ajustes -->
        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="130dp"
            android:layout_height="120dp"
            android:layout_gravity="center_horizontal|bottom"
            android:background="@android:color/transparent"
            android:contentDescription="Ajustes"
            android:onClick="abrirAjustes"
            android:padding="16dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ajustes" />
        <ImageButton
            android:id="@+id/btnCamara"
            android:layout_width="130dp"
            android:layout_height="120dp"
            android:layout_gravity="center_horizontal|bottom"
            android:background="@android:color/transparent"
            android:contentDescription="Ajustes"
            android:onClick="abrirCamara"
            android:padding="16dp"
            android:scaleType="fitCenter"
            android:src="@drawable/camara" />
    </GridLayout>


    <!-- ACDC -->
    <ImageButton
        android:id="@+id/btnACDC"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="10dp"
        android:background="@android:color/transparent"
        android:contentDescription="acdc"
        android:onClick="abrirACDC"
        android:padding="16dp"
        android:scaleType="fitCenter"
        android:src="@drawable/acdc" />

</LinearLayout>
